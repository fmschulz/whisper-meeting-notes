# Copy to ~/.cloudflared/config.yml and replace placeholders.
tunnel: <TUNNEL-UUID>
credentials-file: /home/<user>/.cloudflared/<TUNNEL-UUID>.json

ingress:
  - hostname: nb.newlineages.com
    service: http://127.0.0.1:8891
  - hostname: voila.newlineages.com
    service: http://127.0.0.1:8866
  - service: http_status:404
