# ╔══════════════════════════════════════════════════════════════════════════╗
# ║                    NEXUS Starship Prompt - Cyberpunk Theme                  ║
# ╚══════════════════════════════════════════════════════════════════════════╝

# Prompt format with powerline symbols
format = """
[╭─](bold #ff6600)\
$username\
$hostname\
$localip\
$shlvl\
$singularity\
$kubernetes\
$directory\
$vcsh\
$git_branch\
$git_commit\
$git_state\
$git_metrics\
$git_status\
$hg_branch\
$docker_context\
$package\
$c\
$cmake\
$cobol\
$daml\
$dart\
$deno\
$dotnet\
$elixir\
$elm\
$erlang\
$golang\
$guix_shell\
$haskell\
$haxe\
$helm\
$java\
$julia\
$kotlin\
$lua\
$nim\
$nodejs\
$ocaml\
$opa\
$perl\
$php\
$pulumi\
$purescript\
$python\
$raku\
$rlang\
$red\
$ruby\
$rust\
$scala\
$swift\
$terraform\
$vlang\
$vagrant\
$zig\
$buf\
$nix_shell\
$conda\
$meson\
$spack\
$memory_usage\
$aws\
$gcloud\
$openstack\
$azure\
$env_var\
$crystal\
$custom\
$sudo\
$cmd_duration\
$line_break\
[╰─](bold #ff6600)\
$jobs\
$battery\
$time\
$status\
$container\
$shell\
$character"""

# Color palette matching NEXUS theme
palette = "nexus"

# Fast prompt mode
add_newline = true
scan_timeout = 30
command_timeout = 500

[palettes.nexus]
orange = "#ff6600"
cyan = "#01cdfe"
blue = "#05ffa1"
deep_orange = "#663300"
red = "#ff2a6d"

# ━━━━━━━━━━━━━━━━━━━━━ Core Modules ━━━━━━━━━━━━━━━━━━━━━

[username]
style_user = "bold cyan"
style_root = "bold red"
format = "[$user]($style)"
show_always = true
disabled = false

[hostname]
ssh_only = false
format = "[@$hostname](bold blue) "
ssh_symbol = " "
trim_at = ".local"
disabled = false

[directory]
style = "bold #ff6600"
format = "[$path]($style)[$read_only]($read_only_style) "
truncation_length = 3
truncation_symbol = "…/"
truncate_to_repo = true
fish_style_pwd_dir_length = 0
use_logical_path = true
read_only = " 󰌾"
read_only_style = "red"
home_symbol = " ~"

[directory.substitutions]
"Documents" = "󰈙 "
"Downloads" = " "
"Music" = " "
"Pictures" = " "
"Videos" = " "

# ━━━━━━━━━━━━━━━━━━━━━ Git Configuration ━━━━━━━━━━━━━━━━━━━━━

[git_branch]
symbol = " "
style = "bold green"
format = "on [$symbol$branch(:$remote_branch)]($style) "
truncation_length = 30
truncation_symbol = "…"
only_attached = false
ignore_branches = []

[git_status]
style = "bold red"
format = '([$all_status$ahead_behind]($style) )'
conflicted = "⚔️ "
ahead = "⇡${count}"
behind = "⇣${count}"
diverged = "⇕⇡${ahead_count}⇣${behind_count}"
up_to_date = ""
untracked = "?${count}"
stashed = "📦${count}"
modified = "!${count}"
staged = "+${count}"
renamed = "»${count}"
deleted = "✘${count}"
typechanged = ""
disabled = false

[git_commit]
format = "[\\($hash$tag\\)]($style) "
style = "green"
only_detached = true
disabled = false
tag_symbol = " 🏷  "
tag_disabled = false

[git_state]
format = '\([$state( $progress_current/$progress_total)]($style)\) '
style = "bright-black"
rebase = "REBASING"
merge = "MERGING"
revert = "REVERTING"
cherry_pick = "CHERRY-PICKING"
bisect = "BISECTING"
am = "AM"
am_or_rebase = "AM/REBASE"

[git_metrics]
added_style = "bold green"
deleted_style = "bold red"
only_nonzero_diffs = true
format = '([+$added]($added_style) )([-$deleted]($deleted_style) )'
disabled = true

# ━━━━━━━━━━━━━━━━━━━━━ Programming Languages ━━━━━━━━━━━━━━━━━━━━━

[python]
symbol = "🐍 "
format = "via [${symbol}${pyenv_prefix}(${version} )(\\($virtualenv\\) )]($style)"
style = "yellow bold"
pyenv_version_name = false
pyenv_prefix = "pyenv "
python_binary = ["python", "python3", "python2"]
detect_extensions = ["py"]
detect_files = [".python-version", "Pipfile", "__pycache__", "pyproject.toml", "requirements.txt", "setup.py", "tox.ini"]
detect_folders = []
version_format = "v${raw}"
disabled = false

[nodejs]
symbol = "⬢ "
format = "via [$symbol($version )]($style)"
style = "green bold"
detect_extensions = ["js", "mjs", "cjs", "ts", "mts", "cts"]
detect_files = ["package.json", ".node-version", ".nvmrc"]
detect_folders = ["node_modules"]
version_format = "v${raw}"
not_capable_style = "bold red"
disabled = false

[rust]
symbol = "🦀 "
format = "via [$symbol($version )]($style)"
style = "red bold"
detect_extensions = ["rs"]
detect_files = ["Cargo.toml"]
detect_folders = []
version_format = "v${raw}"
disabled = false

[golang]
symbol = "🐹 "
format = "via [$symbol($version )]($style)"
style = "cyan bold"
detect_extensions = ["go"]
detect_files = ["go.mod", "go.sum", "go.work", "glide.yaml", "Gopkg.yml", "Gopkg.lock", ".go-version"]
detect_folders = ["Godeps"]
version_format = "v${raw}"
disabled = false

[docker_context]
symbol = "🐋 "
format = "via [$symbol$context]($style) "
style = "blue bold"
only_with_files = true
detect_extensions = []
detect_files = ["docker-compose.yml", "docker-compose.yaml", "Dockerfile"]
detect_folders = []
disabled = false

# ━━━━━━━━━━━━━━━━━━━━━ System Information ━━━━━━━━━━━━━━━━━━━━━

[memory_usage]
symbol = "󰍛 "
format = "via $symbol[$ram( | $swap)]($style) "
style = "white dimmed"
threshold = 70
disabled = true

[battery]
full_symbol = "🔋"
charging_symbol = "󰂄"
discharging_symbol = "󰂃"
unknown_symbol = "🔋"
empty_symbol = "󰂎"
format = "[$symbol$percentage]($style) "
disabled = false

[[battery.display]]
threshold = 10
style = "bold red"

[[battery.display]]
threshold = 30
style = "bold yellow"

[[battery.display]]
threshold = 100
style = "bold green"

[time]
format = "🕐[$time]($style) "
style = "yellow"
use_12hr = false
time_format = "%T"
utc_time_offset = "local"
disabled = true
time_range = "-"

# ━━━━━━━━━━━━━━━━━━━━━ Performance & Status ━━━━━━━━━━━━━━━━━━━━━

[cmd_duration]
min_time = 2_000
format = "took [$duration]($style) "
style = "yellow bold"
show_milliseconds = false
show_notifications = false
min_time_to_notify = 45_000
# notification_timeout = 45000  # Disabled - remove or set to number

[jobs]
symbol = "✦"
number_threshold = 1
symbol_threshold = 1
format = "[$symbol$number]($style) "
style = "bold blue"

[status]
symbol = "✖"
success_symbol = ""
not_executable_symbol = "🚫"
not_found_symbol = "🔍"
sigint_symbol = "🧱"
signal_symbol = "⚡"
format = "[$symbol$common_meaning$signal_name$maybe_int]($style) "
style = "bold red"
map_symbol = true
disabled = true
recognize_signal_code = true
pipestatus = false
pipestatus_separator = "|"

[character]
format = "$symbol "
success_symbol = "[❯](bold green)"
error_symbol = "[❯](bold red)"
vimcmd_symbol = "[❮](bold #ff6600)"
vimcmd_visual_symbol = "[❮](bold yellow)"
vimcmd_replace_symbol = "[❮](bold #ff6600)"
vimcmd_replace_one_symbol = "[❮](bold #ff6600)"
disabled = false

[shell]
fish_indicator = "🐠"
powershell_indicator = "_"
zsh_indicator = "zsh"
bash_indicator = "bsh"
unknown_indicator = "?"
format = "[$indicator]($style) "
style = "cyan bold"
disabled = true

[sudo]
format = "[as $symbol]($style)"
style = "bold blue"
symbol = "🧙"
disabled = true

[container]
format = '[$symbol \[$name\]]($style) '
style = "red bold dimmed"
symbol = "⬢"
disabled = false

# ━━━━━━━━━━━━━━━━━━━━━ Cloud Providers ━━━━━━━━━━━━━━━━━━━━━

[aws]
format = 'on [$symbol($profile )(\($region\) )(\[$duration\] )]($style)'
symbol = "☁️  "
style = "bold yellow"
disabled = true
expiration_symbol = "X"
force_display = false

[gcloud]
format = 'on [$symbol$account(@$domain)(\($region\))]($style) '
symbol = "☁️ "
style = "blue bold"
disabled = true

[kubernetes]
format = 'on [⛵ ($user on )($cluster in )$context \($namespace\)]($style) '
style = "cyan bold"
disabled = false
detect_extensions = []
detect_files = []
detect_folders = []
contexts = []

# ━━━━━━━━━━━━━━━━━━━━━ Custom Modules ━━━━━━━━━━━━━━━━━━━━━

[custom.nexus]
command = "echo 🚀"
when = true
format = "[$output]($style) "
style = "#ff6600 bold"
disabled = true