#!/usr/bin/env bash
# ╔══════════════════════════════════════════════════════════════════════════╗
# ║                        NEXUS Neofetch - Cyberpunk Theme                     ║
# ╚══════════════════════════════════════════════════════════════════════════╝

print_info() {
    prin "╭─────────────────────────────────────────────────╮"
    prin "│  ${cl2}◢◤ NEXUS SYSTEM INTERFACE ◢◤${cl0}                 │"
    prin "╰─────────────────────────────────────────────────╯"
    info underline

    prin ""
    prin "${cl6}📍 LOCATION${cl0} ➜ Berkeley, California"
    prin "${cl4}🕐 TIME${cl0} ➜ $(date '+%H:%M:%S %Z')"
    prin "${cl5}📅 DATE${cl0} ➜ $(date '+%A, %B %d, %Y')"
    weather=$(curl -s "wttr.in/Berkeley?format=%c+%t" 2>/dev/null || echo "N/A")
    prin "${cl2}🌤  WEATHER${cl0} ➜ $weather"

    prin ""
    info "${cl6}⚡ USER" title
    info "${cl4}󰌢 HOST" model
    info "${cl5} KERNEL" kernel
    info "${cl2} UPTIME" uptime
    info "${cl3}󰏗 PACKAGES" packages
    info "${cl1} SHELL" shell

    prin ""
    prin "╭─────────────────────────────────────────────────╮"
    info "${cl6} RESOLUTION" resolution
    info "${cl4}󰍛 DE/WM" wm
    info "${cl5} THEME" theme
    info "${cl2}󰏘 ICONS" icons
    info "${cl3} TERMINAL" term
    info "${cl1}󰛡 FONT" font
    prin "╰─────────────────────────────────────────────────╯"

    prin ""
    info "${cl6}󰻠 CPU" cpu
    info "${cl4}󰘚 GPU" gpu
    info "${cl5}󰍛 MEMORY" memory
    info "${cl2}󰋊 DISK" disk

    prin ""
    prin "╭─────────────────────────────────────────────────╮"
    prin "│ ${cl1}███${cl2}███${cl3}███${cl4}███${cl5}███${cl6}███${cl7}███${cl0}  NEXUS ONLINE  │"
    prin "╰─────────────────────────────────────────────────╯"
}

# Title
title_fqdn="off"

# Kernel
kernel_shorthand="on"

# Distro
distro_shorthand="off"
os_arch="on"

# Uptime
uptime_shorthand="on"

# Memory
memory_percent="on"
memory_unit="gib"

# Packages
package_managers="on"

# Shell
shell_path="off"
shell_version="on"

# CPU
speed_type="bios_limit"
speed_shorthand="on"
cpu_brand="on"
cpu_speed="on"
cpu_cores="logical"
cpu_temp="C"

# GPU
gpu_brand="on"
gpu_type="all"

# Resolution
refresh_rate="on"

# Gtk Theme / Icons / Font
gtk_shorthand="on"
gtk2="on"
gtk3="on"

# Text Colors
colors=(5 4 6 2 3 1)

# Text Options
bold="on"
underline_enabled="on"
underline_char="─"
separator=" ➜ "

# Color Blocks
block_range=(0 15)
color_blocks="on"
block_width=3
block_height=1
col_offset="auto"

# Progress Bars
bar_char_elapsed="━"
bar_char_total="━"
bar_border="on"
bar_length=15
bar_color_elapsed="6"
bar_color_total="8"

# Info display
cpu_display="infobar"
memory_display="infobar"
battery_display="infobar"
disk_display="infobar"

# Disk configuration
# Show multiple disks/partitions in the welcome output.
# You can add/remove mount points as needed.
disk_show=(
    '/'
    '/home'
    '/mnt/data1'
)

# Append the mount point to the Disk subtitle (e.g., "/", "/home", "/mnt/data1").
disk_subtitle="mount"

# Show percent used for each disk
disk_percent="on"

# Backend Settings
image_backend="ascii"
image_source="auto"

# ASCII Options
ascii_distro="auto"
ascii_colors=(5 4 6 2 3 1)
ascii_bold="on"

# Image Options
image_loop="off"
thumbnail_dir="${XDG_CACHE_HOME:-${HOME}/.cache}/thumbnails/neofetch"
crop_mode="normal"
crop_offset="center"
image_size="auto"
gap=3
yoffset=0
xoffset=0

# Stdout mode
stdout="off"
