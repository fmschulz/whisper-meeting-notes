# Jupyter + Voila via Cloudflare Tunnel (WSU)

The canonical notebook template and setup instructions live on the `wsu` branch.

To use it on another machine:
1. Check out the `wsu` branch and copy `notebooks/` into the target branch.
2. Follow the runbook in `docs/servers.md` on the `wsu` branch.
